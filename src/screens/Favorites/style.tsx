import {StyleSheet} from 'react-native';

const StyleFavorites = StyleSheet.create({
  title: {color: '#e73134', fontSize: 26, fontWeight: 'bold', marginBottom: 20},
  container: {paddingHorizontal: 15},
});

export default StyleFavorites;
